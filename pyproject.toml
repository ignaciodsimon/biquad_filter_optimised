[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "biquad_filter_optimised"
version = "1.3.0"
description = "Biquad filter libary for DSP"
authors = [{name="Joe Simon"}]
readme = "README.md"
requires-python = ">=3.7"
dependencies = []
